<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Steward.App.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Steward.App.ViewModels">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Steward" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="{x:Bind ViewModel.UserName, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" />
            <TextBlock Text="{x:Bind ViewModel.LastCheckedText, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" />
        </StackPanel>

        <TextBlock
            Grid.Row="1"
            Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            TextWrapping="Wrap"
            Visibility="{x:Bind ViewModel.StatusMessageVisibility, Mode=OneWay}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <ComboBox
                Width="360"
                ItemsSource="{x:Bind ViewModel.Installs, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedInstall, Mode=TwoWay}"
                DisplayMemberPath="Flavour" />
            <Button Content="Browse..." Command="{x:Bind ViewModel.BrowseCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="3" Spacing="8">
            <TextBlock Text="{x:Bind ViewModel.SelectedInstall.ClientVersion, Mode=OneWay}" />
            <ListView ItemsSource="{x:Bind ViewModel.SelectedInstall.AddonRows, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:AddonRowViewModel">
                        <Grid Padding="0,8" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind AddonId, Mode=OneWay}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{x:Bind InstalledVersion, Mode=OneWay}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{x:Bind AvailableVersion, Mode=OneWay}" VerticalAlignment="Center" />
                            <Button Grid.Column="3" Content="{x:Bind UpdateLabel, Mode=OneWay}" Command="{x:Bind UpdateCommand}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
